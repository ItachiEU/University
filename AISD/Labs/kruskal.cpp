#include <bits/stdc++.h>

using namespace std;

struct edge
{
   int weight;
   int v1;
   int v2;
   edge(int w, int v1, int v2) : weight(w), v1(v1), v2(v2) {}
};

int main()
{
   ios_base::sync_with_stdio(false);
   int n, m;
   cin >> n >> m;
   vector<edge> edges;
}